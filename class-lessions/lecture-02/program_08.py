# Question: Write a program in that reads a forename, surname and year of birth and display the names and the year
# one after another sequentially.

# Show message to the screen for getting a forename, surname and year of birth.
forename = input('Enter your forename: ')
surname = input('Enter your surename: ')
yearOfBirth = input('Enter your year of birth: ')

# Show the output (display the names and the year one after another sequentially).
print('Your Name: ', forename, surname)
print('Your Year of Birth: ', yearOfBirth)

